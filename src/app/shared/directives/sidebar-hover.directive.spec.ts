import { SidebarHoverDirective } from './sidebar-hover.directive';

describe('SidebarHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarHoverDirective();
    expect(directive).toBeTruthy();
  });
});
